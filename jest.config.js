module.exports = {
    preset: 'ts-jest',
    testTimeout: 120000,
    // testEnvironment: 'node',
    // If you have a "src" folder, uncomment the following lines:
    roots: ['./test'],
    // testMatch: ['<rootDir>/src/**/*.(spec|test).{ts,tsx}'],
};
  